<%= form_for([@article, @article.comments.build]) do |f| %>
  <p>
    <%= f.label :character %><br>
    <%= f.text_field :commenter %>
  </p>
  <p>
    <%= f.label :bio %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
